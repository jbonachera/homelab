{
  "planPath": "docs/superpowers/plans/2026-06-13-home-assistant-timescaledb.md",
  "tasks": [
    {
      "id": 9,
      "subject": "Task 1: NFS Provisioner",
      "status": "pending",
      "description": "**Goal:** Add `nfs-subdir-external-provisioner` as a HelmRelease with Flux variable substitution for the NFS server address and export path, creating the `nfs-client` StorageClass.\n\n**Files:**\n- Create: `kubernetes/infrastructure/sources/nfs-provisioner.yaml`\n- Create: `kubernetes/infrastructure/nfs-provisioner/helmrelease.yaml`\n- Create: `kubernetes/infrastructure/nfs-provisioner/kustomization.yaml`\n- Create: `kubernetes/infrastructure/nfs-provisioner.yaml`\n- Create: `kubernetes/infrastructure/nfs-provisioner/cluster-vars.yaml.example`\n- Modify: `kubernetes/infrastructure/kustomization.yaml`\n- Modify: `.gitignore`\n\n**Acceptance Criteria:**\n- [ ] `yamllint kubernetes/` exits 0\n- [ ] `kubectl get storageclass nfs-client` shows (default)\n- [ ] `kubectl get pods -n nfs-provisioner` shows Running\n\n**Verify:** `mise exec -- yamllint kubernetes/` → no errors; `kubectl get storageclass nfs-client` → (default)\n\n```json:metadata\n{\"files\": [\"kubernetes/infrastructure/sources/nfs-provisioner.yaml\", \"kubernetes/infrastructure/nfs-provisioner/helmrelease.yaml\", \"kubernetes/infrastructure/nfs-provisioner/kustomization.yaml\", \"kubernetes/infrastructure/nfs-provisioner.yaml\", \"kubernetes/infrastructure/nfs-provisioner/cluster-vars.yaml.example\", \"kubernetes/infrastructure/kustomization.yaml\", \".gitignore\"], \"verifyCommand\": \"mise exec -- yamllint kubernetes/ && kubectl get storageclass nfs-client\", \"acceptanceCriteria\": [\"yamllint kubernetes/ exits 0\", \"kubectl get storageclass nfs-client shows (default)\", \"kubectl get pods -n nfs-provisioner shows Running\"]}\n```"
    },
    {
      "id": 10,
      "subject": "Task 2: External Secrets Operator",
      "status": "pending",
      "blockedBy": [9],
      "description": "**Goal:** Deploy ESO via HelmRelease so the cluster can generate and manage secrets in-cluster without any external vault.\n\n**Files:**\n- Create: `kubernetes/infrastructure/sources/external-secrets.yaml`\n- Create: `kubernetes/infrastructure/external-secrets/helmrelease.yaml`\n- Create: `kubernetes/infrastructure/external-secrets/kustomization.yaml`\n- Create: `kubernetes/infrastructure/external-secrets.yaml`\n- Modify: `kubernetes/infrastructure/kustomization.yaml`\n\n**Acceptance Criteria:**\n- [ ] `yamllint kubernetes/` exits 0\n- [ ] `kubectl get pods -n external-secrets` shows all ESO pods Running\n- [ ] `kubectl get crd externalsecrets.external-secrets.io` exists\n\n**Verify:** `mise exec -- yamllint kubernetes/` → no errors; `kubectl get pods -n external-secrets` → all Running\n\n```json:metadata\n{\"files\": [\"kubernetes/infrastructure/sources/external-secrets.yaml\", \"kubernetes/infrastructure/external-secrets/helmrelease.yaml\", \"kubernetes/infrastructure/external-secrets/kustomization.yaml\", \"kubernetes/infrastructure/external-secrets.yaml\", \"kubernetes/infrastructure/kustomization.yaml\"], \"verifyCommand\": \"mise exec -- yamllint kubernetes/ && kubectl get pods -n external-secrets\", \"acceptanceCriteria\": [\"yamllint kubernetes/ exits 0\", \"kubectl get pods -n external-secrets shows all Running\", \"kubectl get crd externalsecrets.external-secrets.io exists\"]}\n```"
    },
    {
      "id": 11,
      "subject": "Task 3: TimescaleDB",
      "status": "pending",
      "blockedBy": [9, 10],
      "description": "**Goal:** Deploy TimescaleDB as a StatefulSet in the `database` namespace with ESO-generated credentials and a ClusterSecretStore for cross-namespace secret access.\n\n**Files:**\n- Create: `kubernetes/infrastructure/timescaledb/namespace.yaml`\n- Create: `kubernetes/infrastructure/timescaledb/init-configmap.yaml`\n- Create: `kubernetes/infrastructure/timescaledb/statefulset.yaml`\n- Create: `kubernetes/infrastructure/timescaledb/service.yaml`\n- Create: `kubernetes/infrastructure/timescaledb/password-generator.yaml`\n- Create: `kubernetes/infrastructure/timescaledb/external-secret.yaml`\n- Create: `kubernetes/infrastructure/timescaledb/eso-reader-rbac.yaml`\n- Create: `kubernetes/infrastructure/timescaledb/cluster-secret-store.yaml`\n- Create: `kubernetes/infrastructure/timescaledb/kustomization.yaml`\n- Create: `kubernetes/infrastructure/timescaledb.yaml`\n- Modify: `kubernetes/infrastructure/kustomization.yaml`\n\n**Acceptance Criteria:**\n- [ ] `yamllint kubernetes/` exits 0\n- [ ] `kubectl get pods -n database` shows `timescaledb-0` Running\n- [ ] `kubectl get secret timescaledb-credentials -n database` exists with keys `username` and `password`\n- [ ] `kubectl get clustersecretstore kubernetes-reader` shows Ready\n\n**Verify:** `mise exec -- yamllint kubernetes/` → no errors; `kubectl get pods -n database` → timescaledb-0 Running\n\n```json:metadata\n{\"files\": [\"kubernetes/infrastructure/timescaledb/namespace.yaml\", \"kubernetes/infrastructure/timescaledb/init-configmap.yaml\", \"kubernetes/infrastructure/timescaledb/statefulset.yaml\", \"kubernetes/infrastructure/timescaledb/service.yaml\", \"kubernetes/infrastructure/timescaledb/password-generator.yaml\", \"kubernetes/infrastructure/timescaledb/external-secret.yaml\", \"kubernetes/infrastructure/timescaledb/eso-reader-rbac.yaml\", \"kubernetes/infrastructure/timescaledb/cluster-secret-store.yaml\", \"kubernetes/infrastructure/timescaledb/kustomization.yaml\", \"kubernetes/infrastructure/timescaledb.yaml\"], \"verifyCommand\": \"mise exec -- yamllint kubernetes/ && kubectl get pods -n database && kubectl get clustersecretstore kubernetes-reader\", \"acceptanceCriteria\": [\"yamllint kubernetes/ exits 0\", \"kubectl get pods -n database shows timescaledb-0 Running\", \"kubectl get secret timescaledb-credentials -n database exists\", \"kubectl get clustersecretstore kubernetes-reader shows Ready\"]}\n```"
    },
    {
      "id": 12,
      "subject": "Task 4: Home Assistant",
      "status": "pending",
      "blockedBy": [11],
      "description": "**Goal:** Deploy Home Assistant in the `home-assistant` namespace with NFS-backed config storage, TimescaleDB as its primary database, and a Traefik Ingress at `homeassistant.homelab.lan`.\n\n**Files:**\n- Create: `kubernetes/apps/home-assistant/namespace.yaml`\n- Create: `kubernetes/apps/home-assistant/pvc.yaml`\n- Create: `kubernetes/apps/home-assistant/external-secret.yaml`\n- Create: `kubernetes/apps/home-assistant/deployment.yaml`\n- Create: `kubernetes/apps/home-assistant/service.yaml`\n- Create: `kubernetes/apps/home-assistant/ingress.yaml`\n- Create: `kubernetes/apps/home-assistant/kustomization.yaml`\n- Create: `kubernetes/apps/home-assistant.yaml`\n- Modify: `kubernetes/apps/kustomization.yaml`\n\n**Acceptance Criteria:**\n- [ ] `yamllint kubernetes/` exits 0\n- [ ] `kubectl get pods -n home-assistant` shows HA pod Running\n- [ ] `kubectl get secret timescaledb-credentials -n home-assistant` exists\n- [ ] `curl -I http://homeassistant.homelab.lan` returns HTTP 200\n\n**Verify:** `mise exec -- yamllint kubernetes/` → no errors; `curl -I http://homeassistant.homelab.lan` → HTTP/1.1 200\n\n```json:metadata\n{\"files\": [\"kubernetes/apps/home-assistant/namespace.yaml\", \"kubernetes/apps/home-assistant/pvc.yaml\", \"kubernetes/apps/home-assistant/external-secret.yaml\", \"kubernetes/apps/home-assistant/deployment.yaml\", \"kubernetes/apps/home-assistant/service.yaml\", \"kubernetes/apps/home-assistant/ingress.yaml\", \"kubernetes/apps/home-assistant/kustomization.yaml\", \"kubernetes/apps/home-assistant.yaml\", \"kubernetes/apps/kustomization.yaml\"], \"verifyCommand\": \"mise exec -- yamllint kubernetes/ && kubectl get pods -n home-assistant && curl -I http://homeassistant.homelab.lan\", \"acceptanceCriteria\": [\"yamllint kubernetes/ exits 0\", \"kubectl get pods -n home-assistant shows HA pod Running\", \"kubectl get secret timescaledb-credentials -n home-assistant exists\", \"curl -I http://homeassistant.homelab.lan returns HTTP 200\"]}\n```"
    }
  ],
  "lastUpdated": "2026-06-13T00:00:00Z"
}
