---
# Copy this file to cluster-vars.yaml, fill in your values, then apply BEFORE
# Flux first reconciles nfs-provisioner (optional: false will block reconciliation if missing):
#   kubectl apply -f kubernetes/infrastructure/nfs-provisioner/cluster-vars.yaml
# cluster-vars.yaml is gitignored — never commit it.
apiVersion: v1
kind: ConfigMap
metadata:
  name: nfs-provisioner-vars
  namespace: flux-system
data:
  NFS_SERVER: "192.168.1.100"   # IP of your NAS / NFS server
  NFS_PATH: "/volume1/k8s"     # NFS export path
