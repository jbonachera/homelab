---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: timescaledb-credentials
  namespace: media
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: kubernetes-reader
    kind: ClusterSecretStore
  target:
    name: timescaledb-credentials
    creationPolicy: Owner
  data:
    - secretKey: username
      remoteRef:
        key: timescaledb-credentials
        property: username
    - secretKey: password
      remoteRef:
        key: timescaledb-credentials
        property: password
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: jellyfin-db-credentials
  namespace: media
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: kubernetes-reader
    kind: ClusterSecretStore
  target:
    name: jellyfin-db-credentials
    creationPolicy: Owner
  data:
    - secretKey: username
      remoteRef:
        key: jellyfin-db-credentials
        property: username
    - secretKey: password
      remoteRef:
        key: jellyfin-db-credentials
        property: password
